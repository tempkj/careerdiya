# Planning (versioned process)

The implementation plan is versioned alongside the code — like everything else, it changes through
PRs, not in a drifting external doc. Sprint files are the working plan; `Backlog.md` is the ordered
queue mapped to the Phase 1–3 roadmap.
