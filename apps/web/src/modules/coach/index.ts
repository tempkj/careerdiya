// Public surface of the 'coach' module. Other modules import ONLY from here (invariant A1).
// Internals stay private to this folder.
// coach — Phase 2 — single agent + tools; emits signals, never writes the Twin.
export {};
