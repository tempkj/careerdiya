// Public entry for API types. The generated module lives in ../generated (gitignored),
// produced by `pnpm codegen` from the FROZEN contract. Until first codegen this falls back
// to a placeholder type so the import path stays valid.
export type * from "../generated/generated";
