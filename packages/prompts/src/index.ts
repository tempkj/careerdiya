// Loader/registry surface for prompt templates. In Phase 2 this resolves a version id
// (e.g. "coach-prompt/v3") to its template + hash for prompt_registry. Stub for now.
export type PromptVersion = string;
export {};
